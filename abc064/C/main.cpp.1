#include <bits/stdc++.h>
using namespace std;
# define ll long long
# define rep(i,n) for(int i = 0; i < (n); ++i)
# define ALL(obj) (obj).begin(), (obj).end()
# define FAST ios::sync_with_stdio(false); cin.tie(nullptr);
# define _GLIBCXX_DEBUG
# define Pll pair<ll, ll>
#define P pair<int,int>
template<class T>bool chmax(T &a, const T &b) { if (a<b) { a=b; return 1; } return 0; }
template<class T>bool chmin(T &a, const T &b) { if (b<a) { a=b; return 1; } return 0; }
int INF = 1 << 30;

int main(void){
    FAST;
    int n;
    cin >> n;
    vector<int> a(n);
    vector<int> color(9);
    rep(i, n){
        cin >> a[i];
        if(1 <= a[i] && a[i] <= 399) color[0]++;
        else if(400 <= a[i] && a[i] <= 799) color[1]++;     
        else if(800 <= a[i] && a[i] <= 1199) color[2]++;     
        else if(1200 <= a[i] && a[i] <= 1599) color[3]++;     
        else if(1600 <= a[i] && a[i] <= 1999) color[4]++;     
        else if(2000 <= a[i] && a[i] <= 2399) color[5]++;
        else if(2400 <= a[i] && a[i] <= 2799) color[6]++;     
        else if(2800 <= a[i] && a[i] <= 3199) color[7]++;
        else color[8]++;
    }
    int ma = 0;
    rep(i, 8) if(color[i] != 0) ma++;
    ma += color[8];
    int mi = 0;
    rep(i, 8) if(color[i] != 0) mi++;
    if(mi == 0 && color[8] != 0) mi = 1;
    cout << mi << " " << ma << endl;
    return 0;
}